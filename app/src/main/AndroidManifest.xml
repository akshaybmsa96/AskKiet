<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akki.prou">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/eg1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AskQuestionActivity" />
        <activity android:name=".ViewQuestions" />
        <activity android:name=".QuestionFull" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntoActivity" />
        <activity android:name=".AddReplyActivity" />
        <activity android:name=".MyQuestionsActivity"></activity>
    </application>

</manifest>